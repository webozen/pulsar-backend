services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: pulsar
      MYSQL_DATABASE: pulsar_platform
    ports: ["3316:3306"]
    volumes:
      - pulsar-mysql:/var/lib/mysql

  redis:
    image: redis:7-alpine
    ports: ["6380:6379"]

volumes:
  pulsar-mysql:
